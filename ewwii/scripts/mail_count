#!/bin/sh
# mail_count — print count of NEW (unread) messages in maildir.
# Empty output -> widget hides (maildir doesn't exist on this host).
DIR="${MAIL_DIR:-$HOME/.maildir}"
[ -d "$DIR/new" ] || { echo ""; exit 0; }
find "$DIR/new" -maxdepth 1 -type f 2>/dev/null | wc -l
